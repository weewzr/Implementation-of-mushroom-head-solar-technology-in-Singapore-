use std::{env,fs,path::Path};
use mushroom_solar::{weather::parse_nasa_power_hourly_csv,spa::{SpaInput,solar_position},irradiance::isotropic_poa};
const ALBEDO:f64=0.20;
fn parse_stamp(s:&str)->(i32,u8,u8,u8){(s[0..4].parse().unwrap(),s[5..7].parse().unwrap(),s[8..10].parse().unwrap(),s[11..13].parse().unwrap())}
fn svg_bars(path:&Path,monthly:&[[f64;4];12]){
 let w=1000.;let h=520.;let ml=70.;let mb=55.;let pw=w-100.;let ph=h-90.;let max=monthly.iter().map(|x|x[3]).fold(0f64,f64::max)*1.1;
 let mut s=format!("<svg xmlns='http://www.w3.org/2000/svg' width='{w}' height='{h}' viewBox='0 0 {w} {h}'><rect width='100%' height='100%' fill='white'/><text x='500' y='28' text-anchor='middle' font-family='sans-serif' font-size='18'>NASA POWER 2024 development baseline — horizontal POA</text>");
 for m in 0..12{let x=ml+(m as f64+0.15)*pw/12.;let bw=0.7*pw/12.;let mut y=h-mb;for k in 0..3{let bh=monthly[m][k]/max*ph;y-=bh;let fill=["#4472c4","#70ad47","#ed7d31"][k];s+=&format!("<rect x='{x:.1}' y='{y:.1}' width='{bw:.1}' height='{bh:.1}' fill='{fill}'/>");}s+=&format!("<text x='{:.1}' y='490' text-anchor='middle' font-family='sans-serif' font-size='12'>{}</text>",x+bw/2.,m+1);}
 s+="<text x='15' y='260' transform='rotate(-90 15 260)' text-anchor='middle' font-family='sans-serif' font-size='13'>Incident energy (Wh/m²/month)</text><text x='760' y='50' font-family='sans-serif' font-size='12'>direct / diffuse / ground (ground=0 for horizontal)</text></svg>";fs::write(path,s).unwrap();
}

fn svg_sweep(path:&Path,configs:&[(f64,f64)],annual:&[[f64;4]]){let w=1000.;let h=560.;let ml=75.;let _mb=65.;let pw=880.;let ph=430.;let min=annual.iter().map(|x|x[3]).fold(f64::INFINITY,f64::min)*0.98;let max=annual.iter().map(|x|x[3]).fold(0f64,f64::max)*1.02;let mut s=format!("<svg xmlns='http://www.w3.org/2000/svg' width='{w}' height='{h}'><rect width='100%' height='100%' fill='white'/><text x='500' y='28' text-anchor='middle' font-family='sans-serif' font-size='18'>NASA POWER 2024 development fixed-plane sweep (not a ranking)</text>");for (ai,az) in [0.,90.,180.,270.].iter().enumerate(){let mut pts=String::new();for (i,(t,a)) in configs.iter().enumerate(){if (*a-*az).abs()<1e-9&&*t>0.0{let x=ml+(*t/35.)*pw;let y=35.+(max-annual[i][3])/(max-min)*ph;pts+=&format!("{x:.1},{y:.1} ");}}let stroke=["#4472c4","#70ad47","#ed7d31","#a5a5a5"][ai];s+=&format!("<polyline fill='none' stroke='{stroke}' stroke-width='2' points='{pts}'/><text x='{}' y='{}' font-family='sans-serif' font-size='12'>{}° az</text>",790,55+18*ai,az);}for t in [0.,5.,10.,15.,20.,25.,30.,35.]{let x=ml+(t/35.)*pw;s+=&format!("<text x='{x:.1}' y='525' text-anchor='middle' font-family='sans-serif' font-size='12'>{t:.0}</text>");}s+="<text x='500' y='550' text-anchor='middle' font-family='sans-serif' font-size='13'>Fixed tilt (degrees)</text><text x='18' y='270' transform='rotate(-90 18 270)' text-anchor='middle' font-family='sans-serif' font-size='13'>Annual incident energy (Wh/m²)</text></svg>";fs::write(path,s).unwrap();}
fn main(){
 let input=env::args().nth(1).expect("usage: annual-development-baseline POWER.csv OUTDIR");let out=env::args().nth(2).unwrap_or("development-baseline-results".into());fs::create_dir_all(&out).unwrap();
 let raw=fs::read_to_string(input).unwrap();let(records,q)=parse_nasa_power_hourly_csv(&raw).unwrap();assert_eq!(records.len(),8784);assert!(q.is_empty());
 let configs:[(f64,f64);29]=[(0.,0.),(5.,0.),(5.,90.),(5.,180.),(5.,270.),(10.,0.),(10.,90.),(10.,180.),(10.,270.),(15.,0.),(15.,90.),(15.,180.),(15.,270.),(20.,0.),(20.,90.),(20.,180.),(20.,270.),(25.,0.),(25.,90.),(25.,180.),(25.,270.),(30.,0.),(30.,90.),(30.,180.),(30.,270.),(35.,0.),(35.,90.),(35.,180.),(35.,270.)];
 let mut month=vec![[[0f64;4];12];configs.len()];let mut annual=vec![[0f64;4];configs.len()];let mut subhour=vec![[0f64;4];configs.len()];let mut night_direct_max=0f64;let mut source_ghi_wh_m2=0f64;let mut horizontal_dhi_clip_hours=0usize;let mut horizontal_dhi_clip_wh_m2=0f64;
 for rec in &records{let(y,m,d,h)=parse_stamp(&rec.timestamp);let pressure=rec.air_pressure_pa.unwrap_or(101000.)/100.;let pos=solar_position(&SpaInput{year:y,month:m,day:d,hour:h,minute:30,second:0.,utc_offset_h:0.,delta_t_s:69.,longitude_deg_east:103.8198,latitude_deg:1.3521,elevation_m:25.8,pressure_mbar:pressure,temperature_c:rec.ambient_temperature_c});
  let zen=pos.zenith_deg.to_radians();let az=pos.azimuth_deg.to_radians();let sun_up=pos.zenith_deg<90.;let dni=if sun_up{rec.dni_w_m2}else{0.};source_ghi_wh_m2+=rec.ghi_w_m2;
  for(ci,(tilt,face))in configs.iter().enumerate(){let vals=if ci==0{let direct=(rec.ghi_w_m2-rec.dhi_w_m2).max(0.0);let diffuse=rec.ghi_w_m2-direct;if rec.dhi_w_m2>rec.ghi_w_m2{horizontal_dhi_clip_hours+=1;horizontal_dhi_clip_wh_m2+=rec.dhi_w_m2-rec.ghi_w_m2;}[direct,diffuse,0.0,rec.ghi_w_m2]}else{let p=isotropic_poa(tilt.to_radians(),face.to_radians(),zen,az,dni,rec.ghi_w_m2,rec.dhi_w_m2,ALBEDO).unwrap();[p.direct_w_m2,p.sky_diffuse_w_m2,p.ground_diffuse_w_m2,p.global_w_m2]};if ci>0&&!sun_up{night_direct_max=night_direct_max.max(vals[0]);}for k in 0..4{month[ci][m as usize-1][k]+=vals[k];annual[ci][k]+=vals[k];}
   if ci==0{subhour[ci][3]+=rec.ghi_w_m2;subhour[ci][0]+=vals[0];subhour[ci][1]+=vals[1];}
   else{for minute in [7u8,22,37,52]{let sp=solar_position(&SpaInput{year:y,month:m,day:d,hour:h,minute,second:30.,utc_offset_h:0.,delta_t_s:69.,longitude_deg_east:103.8198,latitude_deg:1.3521,elevation_m:25.8,pressure_mbar:pressure,temperature_c:rec.ambient_temperature_c});let zz=sp.zenith_deg.to_radians();let aa=sp.azimuth_deg.to_radians();let dd=if sp.zenith_deg<90.{rec.dni_w_m2}else{0.};let pp=isotropic_poa(tilt.to_radians(),face.to_radians(),zz,aa,dd,rec.ghi_w_m2,rec.dhi_w_m2,ALBEDO).unwrap();subhour[ci][0]+=pp.direct_w_m2*0.25;subhour[ci][1]+=pp.sky_diffuse_w_m2*0.25;subhour[ci][2]+=pp.ground_diffuse_w_m2*0.25;subhour[ci][3]+=pp.global_w_m2*0.25;}}
  }
 }
 assert!(night_direct_max<1e-12);for ci in 0..configs.len(){for k in 0..4{let sm:f64=month[ci].iter().map(|x|x[k]).sum();assert!((sm-annual[ci][k]).abs()<1e-7*annual[ci][k].abs().max(1.));}}
 assert!(annual[0][2].abs()<1e-9);assert!((annual[0][3]-source_ghi_wh_m2).abs()<1e-6);assert!((annual[0][0]+annual[0][1]-source_ghi_wh_m2).abs()<1e-6); // horizontal source-energy identity
 let mut a=String::from("dataset,status,tilt_deg,azimuth_deg,direct_wh_m2,diffuse_wh_m2,ground_wh_m2,total_wh_m2\n");for(ci,(t,z))in configs.iter().enumerate(){a+=&format!("NASA_POWER_SINGAPORE_2024,DEVELOPMENT_NOT_SERIS,{t},{z},{:.6},{:.6},{:.6},{:.6}\n",annual[ci][0],annual[ci][1],annual[ci][2],annual[ci][3]);}fs::write(Path::new(&out).join("annual_poa_sweep.csv"),a).unwrap();
 let mut mo=String::from("dataset,status,month,tilt_deg,azimuth_deg,direct_wh_m2,diffuse_wh_m2,ground_wh_m2,total_wh_m2\n");for(ci,(t,z))in configs.iter().enumerate(){for m in 0..12{mo+=&format!("NASA_POWER_SINGAPORE_2024,DEVELOPMENT_NOT_SERIS,{},{t},{z},{:.6},{:.6},{:.6},{:.6}\n",m+1,month[ci][m][0],month[ci][m][1],month[ci][m][2],month[ci][m][3]);}}fs::write(Path::new(&out).join("monthly_poa_sweep.csv"),mo).unwrap();
 svg_bars(&Path::new(&out).join("horizontal_monthly_components.svg"),&month[0]);svg_sweep(&Path::new(&out).join("fixed_tilt_azimuth_sweep.svg"),&configs,&annual);
 let mut max_midpoint_rel=0f64;let mut max_midpoint_cfg=(0f64,0f64);for ci in 1..configs.len(){let rel=(annual[ci][3]-subhour[ci][3]).abs()/subhour[ci][3];if rel>max_midpoint_rel{max_midpoint_rel=rel;max_midpoint_cfg=configs[ci];}}assert!(max_midpoint_rel<0.005,"hourly midpoint SPA sensitivity exceeds 0.5%: {}",max_midpoint_rel);
 let mut sens=String::from("dataset,status,tilt_deg,azimuth_deg,hourly_midpoint_wh_m2,quarter_hour_quadrature_wh_m2,relative_difference\n");for ci in 0..configs.len(){let rel=if subhour[ci][3]>0.0{(annual[ci][3]-subhour[ci][3])/subhour[ci][3]}else{0.0};sens+=&format!("NASA_POWER_SINGAPORE_2024,DEVELOPMENT_NOT_SERIS,{},{},{:.6},{:.6},{:.9}\n",configs[ci].0,configs[ci].1,annual[ci][3],subhour[ci][3],rel);}fs::write(Path::new(&out).join("midpoint_sensitivity.csv"),sens).unwrap();
 let checks=format!("dataset=NASA_POWER_SINGAPORE_2024_DEVELOPMENT\nsamples={}\ninterval_hours=1\ninput_irradiance_semantics=POWER hourly Wh/m2; numerically equal to 1-hour mean W/m2 only because dt=1h\nspa_evaluation=hour midpoint UTC\nnight_tilted_direct_max_w_m2={:.12}\nmonthly_annual_conservation=PASS\nhorizontal_ground_limit=PASS\nhorizontal_ghi_energy_identity=PASS\nhorizontal_interval_note=horizontal source GHI is conserved exactly; where POWER DHI exceeds GHI, horizontal diffuse is capped at GHI and direct is zero, with reconciliation reported\nhorizontal_dhi_gt_ghi_hours={}\nhorizontal_dhi_gt_ghi_excess_wh_m2={:.6}\nleap_year_completeness=PASS\nground_albedo_assumption={}\nmidpoint_quarter_hour_max_relative_difference={:.9}\nmidpoint_quarter_hour_worst_tilt_deg={}\nmidpoint_quarter_hour_worst_azimuth_deg={}\nmidpoint_adequacy_threshold=0.005\nmidpoint_adequacy=PASS\nstatus=DEVELOPMENT_NOT_SERIS\n",records.len(),night_direct_max,horizontal_dhi_clip_hours,horizontal_dhi_clip_wh_m2,ALBEDO,max_midpoint_rel,max_midpoint_cfg.0,max_midpoint_cfg.1);fs::write(Path::new(&out).join("checks.txt"),checks).unwrap();
 println!("annual-development-baseline PASS samples={} horizontal_total_wh_m2={:.3}",records.len(),annual[0][3]);
}